export interface IPost {
  /**
   * Interface ici est utilisé pout contractualisé une structure
   * elle n'est pas fonctionnelle (permet d'implémenter une structure)
   */
  userId: number;
  id: number;
  title: string;
  body: string;
}
